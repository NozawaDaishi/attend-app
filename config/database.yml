default: &default
  adapter: mysql2
  encoding: utf8
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: root
  password: password
  host: localhost

development:
  <<: *default
  database: attend_development

test:
  <<: *default
  database: attend_test

production:
  <<: *default
  database: attend_production